# Historical Evidence Vault

Acquisition, not T5 activation. Not a Force. No tickers. No prosecutor.

FS-0001 T5 remains **NO_RESULT**. Collecting history does not unlock instruments.

Layers:

1. `raw/` — never modified source files + provenance
2. `harmonized/` — unit conversions only
3. `observables/` — efficiency = tkm/energy; unit_cost = currency/tkm; aggregate_use = tkm
4. `metadata/` — coverage, vintage, breaks, missingness

Rules:

- Pre-acquisition is allowed.
- Post-hoc definition changes based on what we find are not.
- World Bank is a cross-check, not a quiet primary replacement.
- Comparability beats antiquity. Do not stitch a 1970 Frankenstein panel.
- Do not search history for episodes that make FS-0001 look good.
- `T5_READY` stays false until the preregistered contract is satisfied.
