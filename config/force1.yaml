# Force 1 — AI Infrastructure / Memory Bottleneck (v1 Phase A)
# Adjusted ticket group per 2026-08-19 decision.
# Core residual members have multi-year history; DRAM is secondary (short history).

force_id: ai_infra_memory_bottleneck
lead_lag_weeks: 4
version: "2026-08-19-phase-a"

ticket_group:
  # Core residual legs (equal-weight residual vs VOO)
  legs:
    - MAGS   # Roundhill Magnificent Seven (equal-weight Mag7)
    - SMH    # VanEck Semiconductor (concentrated global-ish)
    - SPMO   # Invesco S&P 500 Momentum (currently semi/tech heavy)
  controls:
    - VOO    # Vanguard S&P 500 — primary control / residual baseline
  # Secondary / leading-indicator only (too new for long history residual)
  secondary:
    - DRAM   # Roundhill Memory ETF (launched 2026-04-02)

prices:
  tickers:
    - MAGS
    - SMH
    - SPMO
    - VOO
    - DRAM
    # keep a few legacy for robustness / future orthogonal tests
    - SPY
    - QQQ

panel:
  residual_windows_weeks: [4, 12, 26]
  coherence_lookback_days: 60
  # simple residual = equal-weight leg returns - control returns
  # Phase later: beta-adjusted / multi-factor residualization

naming:
  terms:
    - "memory bottleneck"
    - "HBM shortage"
    - "AI infrastructure"
    - "high bandwidth memory"
    - "DRAM cycle"
    - "semiconductor memory"
  control_terms:
    - "Magnificent 7"
    - "soft landing"
    - "rate cut"

# Phase A focuses only on prices + residual + coherence.
# COT / FRED / flows remain available but secondary for this iteration.
