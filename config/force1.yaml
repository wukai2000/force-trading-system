# Force 1 — AI Infrastructure / Memory Bottleneck
# STATUS 2026-08-24: FALSIFIED — PAUSED. Do not iterate this basket.
# Clean residual IR vs VOO+QQQ = 0.003 (gate was IR ≥ 0.40).
# Basket ≈ 1.24× QQQ beta. Option B (MU/HBM vs SMH) was considered and REJECTED
# to avoid post-hoc specification search. Force 2 was then scanned and also
# failed its pre-registered gate (IR 0.013). Both paused.

force_id: ai_infra_memory_bottleneck
status: falsified_paused
lead_lag_weeks: 4
version: "2026-08-24-paused"
paused_reason: "clean IR 0.003 after 60d OLS vs VOO+QQQ; pre-registered gate IR>=0.40 failed"

ticket_group:
  legs:
    - MAGS
    - SMH
    - SPMO
  controls:
    - VOO
  secondary:
    - DRAM

prices:
  tickers:
    - MAGS
    - SMH
    - SPMO
    - VOO
    - DRAM
    - SPY
    - QQQ

panel:
  residual_windows_weeks: [4, 12, 26]
  coherence_lookback_days: 60

naming:
  terms:
    - "memory bottleneck"
    - "HBM shortage"
    - "AI infrastructure"
    - "high bandwidth memory"
    - "DRAM cycle"
    - "semiconductor memory"
  control_terms:
    - "Magnificent 7"
    - "soft landing"
    - "rate cut"
