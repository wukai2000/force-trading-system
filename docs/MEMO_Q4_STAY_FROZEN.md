# Q4 stay-frozen — three notes compared (2026-09-22)

Operational comparison. Not a score. Not a new episode. Not a package.
Scientific lock remains `c7f21fa`. Capital `$0`. n_seeds=0.

## What each note is

| Source | What it wanted | Status here |
|---|---|---|
| Note 1 | Hold freeze. Idle ≠ unimplemented already on `desk.yaml`. A/B/C fully open. No new scientific doc. Drive pointer stays the 2026-09-19 package. | **Keep.** This is the law. Overlay is `426f104`, not a thaw. |
| Note 2 | One anti-drift invariant. IDLE ≠ UNIMPLEMENTED. Household default A, but capital stays `$0`. CI that only reports PASS/FAIL. | **Keep the test.** Dismiss the second schema and the desk lock on A. |
| Note 3 | Lock Option A. New `docs/FREEZE_PERMITS.md`. `test_freeze_assert.py` against root `desk.yaml`. HEAD `~58cc2bf`. | **Dismiss.** Wrong path, wrong SHA, and a household lock smuggled into the freeze. |

## Keep

1. `next_object: stay_frozen`. `architecture_status: idle_not_unimplemented`.
2. Idle is true only when the measurement layer is observed **and** the identifying experiment on this frozen object cannot complete **and** a second instance is refused.
3. Permits/forbids stay keys on the overlay. They are not a new scientific document.
4. Partial scoring stays forbidden (`score_2014_only`, `drop_five_and_score_15_of_20`).
5. Scientific lock is `c7f21fa`. Later HEAD commits that only assert the freeze are not a new lock.
6. Household A/B/C stays open. Trump Account remains SPYM-only. That is not a Force decision and not Option A locked by the desk.
7. Drive pointer stays `force-trading-system-20260919-e1i-fail-scope.tar.gz`. No new scientific tarball.
8. SE-AIS-01 stays Gatekeeper, not a freeze.

## Dismiss

| Item | Why |
|---|---|
| Nested `stay_frozen:` / `freeze_policy:` | Duplicates keys already on the overlay. A second schema is drift. |
| `docs/FREEZE_PERMITS.md` | Promotes the card into a new document. Note 1 forbids that. |
| Locking household Option A | Zero scientific object is not a household default. |
| Root-path `tests/test_freeze_assert.py` | Desk lives at `force_ideas/desk.yaml`. Suite is `scripts/test_*.py`. |
| HEAD `58cc2bf` or `c5bcc5a` as the scientific lock | Chore and learn/dismiss. Lock is `c7f21fa`. |
| Daily job that emits a new scientific artifact | Heartbeat belongs in the existing test, not a new object. |
| New Drive package | No new scientific object. |

## What this pass adds

`scripts/test_stay_frozen.py` asserts the overlay already written. It does not change the frozen Force, the vintages, or the capital.

Next: `stay_frozen`. Silent default = later. Q4 stay frozen.
