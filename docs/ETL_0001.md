# ETL-0001 — Economic Temporal Language experiment

Not a language claim. Not a Force. Not an amendment of BR-0001.
Capital $0. Promotion `NOT_PERMITTED`.

**Definition used:** ETL is a *candidate statistical object*. It exists only if a locked encoding yields transition constraints that beat Markov-1 and order-destroying nulls on a predeclared holdout. Frequency is not grammar.

## Consolidation (what was kept)

| Source | Kept | Dropped |
|---|---|---|
| Locked-object note | 3 series, 4 symbols, k=2–3, ≤20 composites, pairwise first, holdout ΔNLL | Treating markets as a language |
| ETL Architecture v1 | Encoding hashed first; R1–R8; OTHER/RARE; N1–N5; separate interpretation desk | Open motif mining |
| Hostile audit | ETL as candidate not a thing; two graphs (research vs evidence); synthetic must fail; no-grammar is a result | Rolling 1900 windows in v1; continuous trajectories in v1 |
| TSG v0.2 | H1 ≠ H2; EFE ≠ Force ID; incremental information hard gate; Output A/B | 15-stage p-decoration; Λ>100 as if run; C/D letters |
| Proposal-bus rule | Inference may travel; evidence may not | Using a story to recode L1 |

BR-0001 stays the first-print crude tape. ETL-0001 is a *different* encoding on monthly FRED macros. Do not mix ledgers.

## Encoding (frozen)

`force_ideas/inventory/etl_0001.yaml`

- INDPRO, UNRATE, CPIAUCSL monthly
- `{D,F,R,U}` from past-only 60-month σ of first differences, `c=0.75`
- Prefix 1985–2009 / holdout 2010–2019 / stress 2020–2023 report-only
- Snapshot is **as-revised FRED → label `LEAKY`**. Not confirmatory.

## v1 result (this commit)

Synthetic IID: order-2 does **not** beat Markov-1 (pipeline not broken).

Leaky FRED pairs: order-2 holdout NLL *worse* than Markov-1 on all three pairs. Prefix Markov-1 itself loses to a prefix-marginal iid predictor on holdout (non-stationary token frequencies).

Label: **`NO_HIGHER_ORDER_STRUCTURE`** for this encoding + these series.
That falsifies *this* discretization, not the hypothesis class.

## Still next on crude

Continue Wednesday issue files beyond July 2014. 2011 Jan–Jul still missing on EIA HTML archive. Do not write `e1i_vintages.ok`.
