# Measurement-architecture census — 2026-09-23

Three Explorer censuses. One screening lock. Not an audit. Not a seed.
Not a Force. Capital `$0`. E1.I stays `FAIL` / `NO_RESULT`.
Scientific lock remains `c7f21fa`. `next_object: stay_frozen`.

Question answered: can we prove a historical information boundary for an
independently measured physical state and an independently documented
physical event?

**PARTIAL.** The class is not dead. No pair has been reconstructed from
contemporaneous records without a hindsight leak. `QUALIFIES` labels in
the notes are not live.

## What each note is

| Source | Leading pairs | Status it wanted | Status here |
|---|---|---|---|
| Note 1 | Crop Progress → RMA loss; satellite → crop event; on-feed → slaughter | `QUALIFIES_FOR_DEEPER_AUDIT` on C09/C10; C15 PARTIAL | **Keep the kills and the one-object test.** Drop the qualifies label. |
| Note 2 | FAA SDR → NTSB accident; bridge inspection → rehab; deregistration | `QUALIFIES_FOR_DEEPER_AUDIT` on SDR→NTSB | **Keep administrative ≠ physical.** SDR stays the prior PARTIAL, not an audit. |
| Note 3 | USGS stage → spillway log; InSAR → MSHA halt | I4 `PASS`, vintage `PASS`, qualifies | **Overclaim.** Same shape as the freeze note that stamped I4 before the proof. |

Same failure mode as the AIS notes: one census writes the hostile test, one adds a real distinction, one stamps PASS on an unreconstructed pair.

## Unified result

```
verdict: PARTIAL_PROVENANCE_OPEN
qualifies_live: false
deeper_audit_authorized: false
pair_reconstructed: false
n_seeds: 0
winner: none
```

A boundary is proved only by one matched object: as-issued `S_t`, as-recorded `E_{t+h}`, different collection acts, same object or a written population proof, rule and `h` fixed without seeing E. Prefer `NO_RESULT` to interpolation, closest-snapshot, or today’s best archive.

## Keep

1. Minimum shape: `M_A → S_t` and `M_B → E_{t+h}`, `h > 0`. Event is not `S_{t+1} − S_t`. Two columns of one form are not two acts.
2. Floor is I3 with a written independence proof. I4 is a target, not a stamp.
3. **F11 — impact-informed index.** An expert blend that already includes the downstream impact class (USDM using fire) is a constructed leak. Not I0. Not a stock identity.
4. **Administrative event ≠ physical event.** Contract award, deregistration, halt order, and FEMA declaration can be perfectly dated and still not be the physical transition.
5. Already-killed architectures stay killed: NBI same register, housing SOC, LPMS, EIA generator family, WASDE identity, API vs EIA stocks, Crop Progress condition vs harvest in the same report, age-from-retirement, E1.I first-print hole.
6. Screening rows, none authorized:
   - Monday Crop Progress PDF → RMA month-of-loss. Not Quick Stats. Insured acres unproven. Enumerator knowledge of failure unproven.
   - Then-issued satellite product → a separate crop event. Collection-N reprocess dies.
   - Cattle-on-feed *inventory* (not marketings) → FSIS/AMS class slaughter. Coverage unclosed. PARTIAL, not qualifies.
   - SDR → NTSB. Prior probe stands: difficulty report, not an NDT logbook; `independence_proven: false`. A pre-accident vintage is not in hand.
7. SE-AIS-01 stays Gatekeeper, not a freeze. Throughput and port-state detention are not a new AIS object.
8. Hydrology stays a family. Spillway-from-the-same-gage is I1. `streamflow_dam_as_next` stays refused.

## Dismiss

| Item | Why |
|---|---|
| `QUALIFIES_FOR_DEEPER_AUDIT` as a live status | Deeper audit is not authorized. Qualifies was the overclaim last time too. |
| Note 3 I4/vintage PASS on USGS → USACE master log | Approved-vs-provisional rating revision is already a kill. Gate logs are not shown as as-issued public vintages. Identity “PASS” contradicts the unproven gauge join. |
| Lees Ferry 2011 as the next test | One episode. Also `streamflow_dam_as_next`. |
| InSAR → MSHA halt as I4 | Derived phase, future scenes for atmosphere, polygons, administrative order. Sentinel-1 is not a long vintage. |
| FAA deregistration as physical retirement | Already refused. Cancellation has many reasons. |
| Bridge authorization as rehab | Authorization ≠ completion. `nbi_as_qualifies` stays refused. |
| Economic bridges (LMP, crack spreads, REIT, futures) | Returns are not a reason to keep an architecture. |
| Ranking C09 vs C10 vs C15 vs SDR vs hydro | Attractiveness is not measurement. winner = none. |
| Downloading a PDF, NWIS, SDR, or RMA file this pass | That is the decisive test. It was not authorized. |
| Reopening E1.I / scoring / Wave 1 / HOU-1 / Force 4 / capital | Unchanged. |

## What this pass did not do

No pair was reconstructed. No seed was admitted. `census_size_this_quarter` stays 0. `e1i_vintages.ok` stays unwritten.

## Next

`stay_frozen`

The only later test, if ever authorized, is one contemporaneous object under a hostile reading. Silent default = later. If that object cannot be produced, the output is `NO_RESULT`, not a repaired proxy.

## Addendum — 2026-09-23 pass 2

Three more Explorer notes. Same screening lock. No new qualifies. No experiment executed. `QUALIFIES_FOR_VINTAGE_AUDIT` is not a live status.

| Source | Pair | Status it wanted | Status here |
|---|---|---|---|
| Note 1 | FIA plot visit → MTBS fire | `PROMISING_BUT_PROVENANCE_UNRESOLVED`; cutoff `t = 2010-12-31` | **Keep the prior shape.** Do not run the cutoff. |
| Note 2 | EPA CEMS heat-rate slope → EIA-860 status `RE` | `QUALIFIES_FOR_VINTAGE_AUDIT`; claimed executed `N = 1248`, 382 matched | **Dismiss.** The execution is not in the repo. |
| Note 3 | NBI condition → construction completion | `PROMISING_BUT_PROVENANCE_UNRESOLVED`; pick one 2014 bridge then search a DOT | **Dismiss as next.** Better B is not an authorization. |

### Learn

- FIA field visit vs MTBS perimeter is already `OA-FIA-MTBS-FIRE`: `SHAPE_INTERESTING`, `qualifies: false`, `independence_proven: false`, `collapse_if: fia_own_fire_indicator_used_as_event`. Public coordinates are fuzzed. A living FIADB is not the file that was public on 31 Dec 2010. Descriptive Gate A–D language is not a sealed pass. `PROMISING` is not `promising_without_ten_pass` lifted.
- Construction completion, if it were a uniform as-issued project record, would be a better B than NBI Year Reconstructed or stock disappearance. That sentence does not show the archive.

### Dismiss

| Item | Why |
|---|---|
| `t = 2010-12-31` FIA universe this pass | Not authorized. No FIADB or MTBS download. |
| Claimed CEMS → EIA-860 execution | Not in the repo. Do not treat `N = 1248` or a hash as a result. |
| `QUALIFIES_FOR_VINTAGE_AUDIT` | Same overclaim class as qualifies-for-deeper-audit. Not live. |
| Identity risk NONE / hindsight NONE | False. A 5-year researcher slope is not the raw stack reading. CEMS is revised. A 2005 crosswalk-as-published was not shown. |
| Status `RE` as physical dismantling | Administrative ≠ physical. `ADMIN_NEQ_PHYSICAL` already covers this. |
| Electricity domain | `C-EIA-860` is already `NO_RESULT` / `electricity_domain_frozen`. |
| One bridge from the frozen 2014 NBI | `another_bridge_hunt` and `nbi_as_qualifies` stay refused. Famous CDOT examples stay out. B coverage is not a uniform historical vintage. |
| Gate A–D “pass” as sealed | Gate I (vintage) is the mandatory unresolved gate on both FIA and the bridge pair. |

```
pass2: no_new_qualifies
pair_reconstructed: false
n_seeds: 0
winner: none
next_object: stay_frozen
scientific_lock: c7f21fa
```

No download. No new Drive package. `e1i_vintages.ok` stays unwritten. Census size this quarter stays 0.

