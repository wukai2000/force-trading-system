# BR-0001 — Sealed sequential CAP classification

Not a Force. Not a ticker map. Capital $0. Promotion `NOT_PERMITTED`.
Companion specs: `force_ideas/inventory/br_0001.yaml`, `mts_0001.yaml`.
Harness: `scripts/run_br_0001.py` (refuses to score until E1.I vintages exist).

Locked 2026-09-14. Do not retune EP-CRUDE-2014. Do not open a new episode.

## Status

| Gate | Object | Status |
|---|---|---|
| 0 | Freeze A, B, x, rubric, NCs, cutoffs | FROZEN |
| 1 | E1.I first-print vintages | **NOT RUN** |
| 2 | Sealed sequential scoring of `S_T` | **REFUSED** |
| 3 | Finance role-3 incorporation | LOCKED |

E1.I passing is a plumbing assumption in the design memos. It is not a result.
This experiment does not start until Gate 1 is a real vintage tape.

## Addendum 2026-09-19 — Gate 1 FAIL, scope E1.I-local

Gate 1 is `FAIL`. Reconstruction `NO_RESULT`. Scoring stays refused.
Fail scope is `E1I_LOCAL`: first-print measurement exists; the locked
NC grid cannot be completed. Not an architecture kill. Not a 2014-only
score. `e1i_vintages.ok` unwritten. Next: `stay_frozen`.


## Object

Reconstruct the observable state of an unresolved contest between two
precommitted adjustment processes. Do not reconstruct a latent Force.

`Force := CAP candidate` (competing adjustment process). The word Force does
not drive design.

Path C, narrowed:

> At cutoff T, using only `I_T` dated ≤ T, assign `{A, B, U}`. `U` includes
> insufficient information and observational equivalence.

## Frozen hypothesis

There exist two precommitted mechanisms `A`, `B` and one precommitted scalar
`x` such that the sealed state `S_T` is not observationally equivalent to
always-`U` and not observationally equivalent to a persistence null on
`sign(Δx)`.

If `A`, `B`, or `x` cannot be specified without the completed 2014 story,
record `HYPOTHESIS_UNFREEZABLE` and do not run.

### A, B, x (locked; from the petroleum identity, not from the glut narrative)

| Symbol | Meaning |
|---|---|
| A | demand destruction: production ↓ and product supplied ↓ over the locked 4-week cutoff |
| B | lagged supply: production ↑ and product supplied ↓ over the same cutoff |
| U | everything else, including observational equivalence |
| x | 8-week sign of `production_kbd` (the discriminating observation already named in MTS-0001) |

Do not replace `x` with stocks because the as-revised smoke’s only edge was
stocks. That would be a retune.

## State vector (entire output at each cutoff)

```
S_T = (C_T, Λ_T, π_T, F_T)
```

- `C_T ∈ {A, B, U}`
- `Λ_T ∈ {0, 1}` in this version. `Λ_T = 1` forces `C_T = U`. No unfrozen continuous score.
- `π_T ∈ {-, 0, +, NA}`. Predicted sign of `Δx` over the locked 8-week horizon. `NA` iff `C_T = U`.
- `F_T` one frozen sentence:
  - B: next 8-week production change is ≤ 0
  - A: next 8-week production change is ≥ 0
  - U: NO_CALL

No prose diagnosis. No fingerprint. No “A is gaining.”

## Windows

Same as E1.I / EP-CRUDE-2014 / EP-NC-2011. No added cutoffs. No dropped cutoffs.

## Three ledgers (never mixed)

1. **Reconstruction** — after reveal of `Δx`, did `F_T` fire? Did `C_T` stay admissible?
2. **Transition** — among cutoffs with `C_T ≠ U`, is `sign(Δx) = π_T`? Loss is 0–1 on signed calls only. Always-`U` is a legal strategy and can win.
3. **Abstention** — raw rate of `U` / `NO_CALL`. Not a penalty by default.

Map onto Memo 2 propositions: ledger 1 ≈ state reconstruction, ledger 2 ≈ next-state, discrimination is whether `A` vs `B` calls are not interchangeable under NC-5.

## Negative controls (freeze before any score)

| ID | Control | Required |
|---|---|---|
| NC-1 | Permute order of `x`, keep the rubric | Must not recover a confident Path C sequence |
| NC-2 | Irrelevant family, if used | Must not support confident A/B |
| NC-4 | 2011 window already locked as absence/mismatch control | Must not beat persistence by 0.05 |
| NC-5 | Swap names of A and B after lock | Scores must swap |
| NC-6 | First-print vs later-revised values of the same date | Stability only on revised data → `VINTAGE_FAIL` |
| NC-7 | Narrative text only | Text-alone recovery of `C_T` → journalism |

NC-3 (deliberately mismatched pair) is reserved and not required for the first tape.

## Information frontier

Do not maximize lead time. Report the first cutoff `T*` where `C ∈ {A,B}`,
`Λ = 0`, `π` defined, and the next increment does not immediately fire `F_T`.

If `T*` exists only after the public naming of the 2014 glut, the tape is a
restatement (`TOO_LATE`). If no `T*` exists, the frontier is: never identifiable
from the allowed archive.

## Terminal labels

- `PASS_NARROW` — ledgers + NCs hold; `T*` exists and is not TOO_LATE. Still not a Force.
- `UNIDENTIFIABLE` — live tape dominated by honest `U`; NCs behave.
- `FAIL` — confident A/B that lose to persistence / always-U, or `U` used as evasion.
- `NO_RESULT` — the test did not occur in a form that can license any claim.
- `HYPOTHESIS_UNFREEZABLE` — A,B,x needed the ending.
- `KILLED_PATH_C` — narrow Path C fails its falsifiers.
- `REFUSED` — Gate 1 missing (current legal state of the harness).

## Invalidation (not a fail — invalid)

A, B, or x edited after first cutoff opened. Later revisions on the live tape.
Prices. `S_T` edited after reveal. Scorer had the completed narrative. New
observables, cutoffs, or episodes to rescue a tape. Memo 3 MTS-0002 opened.

## Finance

Forbidden until BR-0001 is not `NO_RESULT`, NCs behave, `T*` is before TOO_LATE,
and `UNIDENTIFIABLE` was actually used. First allowed financial role is
**incorporation only** (did quotes already move with `C_{T*}`?). Securities
remain illegal.

## What this file does not do

It does not fetch vintages. It does not score the as-revised smoke again.
It does not promote. It does not rename the repository.
