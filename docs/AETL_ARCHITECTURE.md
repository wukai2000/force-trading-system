# AETL Architecture v0.1 — frozen control plane

Methodological research only. Capital `$0`. EvidenceRecord `NOT_PERMITTED`.
Does not amend BR-0001. Does not reopen ETL-0001. Force language off.

## Consolidation of the three notes

| Keep | Drop |
|---|---|
| Eight primitives, not fifty modules | Scalar Discovery Debt and any formula that turns tries into a fake posterior |
| SearchRecord + search *graph* | “p < 0.01 after MDL” as a promotion rule |
| Governor as control plane | Epistemic Governor as a ninth discovery engine |
| Typed lessons (class-level only) | Candidate residuals back into Explorer |
| Hidden / touch-once CertifierBattery | Prosecutor that invents a test to save *this* candidate |
| Status ladder with reserved Force / finance | Memo 3’s order that puts DISCOVERED *after* STATISTICALLY_SUPPORTED |
| Lab A–F / S0–S8 *before* historical E | Opening FRED or WPSR as certification because the constitution exists |
| Prequential / holdout / surrogate rank | Bonferroni theater with a fake k |
| `NO_RESULT` as a successful terminal | Endless search until something is publishable |

Doc 1 is the specification. Doc 2 adds the governor, information-set (not dataset) separation, and the independence-is-not-binary problem. Doc 3 is a useful parts list; its scalar debt and p-value rungs are rejected.

Central law:

> Anything may propose. Nothing may self-certify.
> Adaptive action creates information history. History determines independence.
> Debt is paid with untouched information, not with a better statistic on searched information.

## Eight primitives

1. Search ledger (`aetl/ledger.py`)
2. Partition + I_t lock
3. Typed feedback (Lesson vs chase)
4. Inference-after-search (exploratory numbers are not certificates)
5. Locked null battery
6. Representation as a candidate (not preprocessing)
7. Semantic one-way lock
8. Promotion + program-level `NO_RESULT`

## Agents

Explorer, Inferencer, Prosecutor, Certifier. Governor enforces transitions.
Human and external models are proposers. They write ledger rows. They never stamp status.

## Statuses this phase may emit

`OBSERVED DERIVED DISCOVERED STATISTICALLY_SUPPORTED REPLICATED INTERPRETED MECHANISTICALLY_SUPPORTED REFUTED UNRESOLVED NO_RESULT REFUSED`

Refused: `FORCE_CANDIDATE`, `FINANCIAL_TRANSMISSION_HYPOTHESIS`.

## What was built

- `aetl/governor.py` — illegal transitions raise; chasing lessons raise; cert partition is touch-once; hindsight cannot be `STATISTICALLY_SUPPORTED`.
- `aetl/ledger.py` — append-only SearchRecord; burden is counts, not a score.
- `aetl/lab_e0.py` — three synthetic worlds, nested M0/M1/M2, locked holdout margin.
  P2 intermittent ABC word: 17/20 `STATISTICALLY_SUPPORTED`.
  P1 first-order cycle: 20/20 `MARKOV1_ONLY` (extra grammar refused).
  IID: 20/20 `REFUTED`.
  Chasing lesson blocked.
- Historical experiment remains **closed**. Realistic-dependence and adversarial labs are not done.

## What E0 does not claim

It does not claim the architecture is certification-safe on economics.
If later B/D labs promote on noise, the *program* is `NO_RESULT` and historical data stays shut.
