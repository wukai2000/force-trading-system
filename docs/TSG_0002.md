# TSG-0002 / TSG-MS-v0.2 — Empirical Force-effect ontology

Supersedes the implicit v0.1 collapse grammar → mechanism → Force.
Does not amend BR-0001. Does not mint a Force ID. Capital $0.
Status: **FROZEN SPEC FOR REVIEW**. Mining still refused. Fitting still refused.

Parent physical tape remains BR-0001. As of this writing Gate 1 is **PARTIAL**
(one Wednesday proven). TSG execution stays `PREMATURE`.

## Governing distinction

Knowledge of an effect can precede knowledge of its cause.
Unknown cause must remain unknown.

```
effect known + cause unresolved  = valid scientific state
effect known + cause unresolved ≠ preferred story
```

## Two axes (not one ladder)

Effect axis: `NO_RESULT` → reproducible structure → `EMPIRICAL_FORCE_EFFECT`

Mechanism axis: `U` / unevaluated → `UNIDENTIFIABLE` → `MECHANISM_SUPPORTED` → `FORCE_IDENTIFIED` (status only)

`EMPIRICAL_FORCE_EFFECT + UNIDENTIFIABLE` is legal.
`UNIDENTIFIABLE` is not a consolation prize and not a downgrade of the effect.

`U` is a Level-1 state token when `I_t` cannot assign A/B.
`UNIDENTIFIABLE` is an H2 label after H1 has already passed.
Do not conflate them.

## Empirical Force-effect (frozen wording)

A reproducible temporal pattern of independently defined observable economic
states whose existence and incremental informational properties survive
pre-specified statistical prosecution, while available information cannot
sufficiently discriminate the generating mechanism.

Forbidden equivalences: not an identified Force, not causal proof, not a
trading signal, not a story, not FS-0002.

The word Force inside the label is tolerated only because the non-equivalences
are binding. In Output A, prefer the token `EMPIRICAL_FORCE_EFFECT` over prose
that says “a Force was found.”

## Incremental information (hard gate)

M0 frequency → M1 persistence → M2 Markov-1 → M3 pre-declared regime/calendar
→ M4 candidate grammar.

M4 wins only if it adds reproducible information beyond the strongest of
M0–M3 on a locked metric, locked partition, after multiplicity control.
Otherwise `NO_HIGHER_ORDER_STRUCTURE` (= species of `NO_RESULT`).

Exact metric family is **not** uniquely mandated in v0.2. It must be locked
before any discovery count. Metric shopping after ranks is `INVALID`.

## Identification (status, not catalog)

`FORCE_IDENTIFIED` requires the conjunctive minimum in the long v0.2 draft:
independent observables, competitor discrimination, mechanism-specific
predictions, hold-out, cross-episode stability, non-circular construction,
residual-alternative disclosure. Recurrence + story is insufficient.
Meeting the status still does not issue FS-0002.

## Information-limit classes (when H2 stops)

IL-1 unmeasured; IL-2 internals; IL-3 incomplete history; IL-4 delay;
IL-5 weak contemporaneous discrimination; IL-6 observational equivalence;
IL-7 revision opacity; IL-8 non-separability.

Diagnose which class binds. Do not shop mechanisms until one more story fits.

Current physical relevance: EIA HTML/CSV archive for 2011 begins 2011-08-03.
Jan–Jul 2011 NC cutoffs are `MISSING_ON_EIA_HTML_ARCHIVE` until Wayback or
another first-print object is bound. That is an IL-3 / IL-7 candidate for the
*tape*, not an H2 result. H2 is not open.

## Outputs

Output A: empirical object. No story. Hash and freeze.
Output B: mechanism analysis only after A is frozen. Cannot edit A.

## What v0.2 does **not** authorize

PrefixSpan, four-representation bake-off, N_min fishing, adding C/D to spell
`A→C→D→B`, Λ>100 as if already computed, 15-stage p-values as decoration,
finance, FS-0001 edit, BR-0001 amendment, writing `e1i_vintages.ok` from one
Wednesday.

## Still next on the physical path

Remaining 2014 warmup + live issue files. Then 2014 grid. Then a 2011 path
that is not the missing HTML archive. Then, only then, M0–M2 on a sealed tape.

Open hypothesis (not doctrine): a Force is a latent generator of transitions.
Attractiveness is not evidence.
