# E1.I fail scope — 2026-09-19

Accepted: Gate 1 `FAIL`. Reconstruction `NO_RESULT`. `e1i_vintages.ok` unwritten.
Not a score. Not a new episode. Not North Carolina. Capital $0.

Authorized question: is the failure local to E1.I, or evidence that the
proposed historical architecture itself has no observable implementation?

## Verdict

**Local to E1.I.** Not an architecture kill.

```
fail_scope: E1I_LOCAL
architecture_observable_implementation: MEASUREMENT_LAYER_YES
identifying_experiment_on_frozen_object: NO
```

## What E1.I is

Same identity, same windows, same rule, true vintages. Both the 2014
primary grid and the 2011 NC grid. Fail shape already written in
`docs/I_T_CONTRACT.md`: seal MTS-0001 as `NO_RESULT`, do not pick a
new episode.

That fail shape fired. Five of twenty NC cutoffs unrecoverable.
`F_t` cannot be built for the locked experiment. `I_t` is not a series.
BR-0001 is not scored.

## Why this is not “the architecture has no observable implementation”

The proposed historical architecture is: competing mechanisms inside an
identity; `F_t` = first-print issue files; `I_t = f(F_t)`; next physical
state versus persist; negative control; returns last.

Its **measurement layer** is observed:

| Tape | What exists |
|---|---|
| 2014–2016 WPSR | 135 issues, 27/27 primary cutoffs bound |
| 2011-08-03 … 2012-06-27 | 48/48 listed archive cells |
| NC cutoffs | 15/20 have an issue file |
| One bind | 2014-07-02 `I_t=U`, unscored |

A Wednesday issue of the three US legs is a real extract. Cells are not
`I_t`. Existence of the extract is the implementation of measurement
(level 0). The architecture is not empty.

The hole is **Jan–Jul 2011 archive coverage** on a locked NC window:
live dated 404, Wayback `id_` → 2011-08-13, Jan 5 D1 orig-lm 30 Dec 2010,
HathiTrust fiche ends 2000, IA collections 0, GovInfo not an issue.
That is E1.I’s frozen NC, not a proof that first-print competing-mechanism
state cannot be read from official issues as a class.

Redesign v2 kill criterion: *if that architecture cannot reconstruct known
history without hindsight, kill it.* We reconstructed 2014 known-episode
**cells**. We did not assemble `I_t` because Gate 1 is joint (primary and
NC). We did not test fingerprint-without-hindsight. Unrun reconstruction
is not a kill.

Census n=0, FS-0001 unit-cost `NOT_CONSTRUCTIBLE`, and MQA qualified=0
are other objects. They are not this result. Mixing them in is domain
shopping.

## Split

| Layer | Status |
|---|---|
| E1 as laboratory step | protocol-open as a class |
| E1.I frozen instance | `SEALED_NO_RESULT` |
| Measurement extract (WPSR cells) | observed where the archive exists |
| Identifying experiment (complete `F_t` including NC → `I_t` series → score) | not constructible on this object |
| Architecture definition | stands; not killed |
| Runnable second instance | refused (`do_not_select_another_episode`) |

## What this is not license to do

- Drop five cutoffs and score 15/20 (retune)
- Score 2014-only (NC is load-bearing for E1.I)
- Pick a new episode or later NC so the architecture can “have an implementation”
- Path D from this FAIL (Path D is leftover-vs-SPY, not first-print existence)
- Reopen census / MQA / FS-0001 cousins
- Write `e1i_vintages.ok`
- Code missing as `U`
- Closest-snapshot as a week
- Promote, sidecar, capital

Under freeze the architecture is **idle**, not unimplemented. Idle because
the frozen instance cannot complete and a second instance is refused.
That is not the same sentence as “no observable implementation.”

## Next

`stay_frozen`

Silent default = later. Q4 stay frozen. n_seeds=0. Capital $0.
